server:
  port: 2222
spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    password: postgres
    url: jdbc:postgresql://localhost:5432/restaurant
    username: postgres
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  jwt:
    secret_key: java-10
